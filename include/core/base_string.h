#pragma once

#include <core/types.h>

namespace falconOS {
namespace core {
int len(char *str_);

char *reverse(char *str_);

char *to_string(const int int_);
} // namespace core
} // namespace falconOS
